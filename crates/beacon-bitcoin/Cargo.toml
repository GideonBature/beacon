[package]
name = "beacon-bitcoin"
description = "Bitcoin-shaped DisputeBackend skeleton for Beacon (simulated chain journal; no BitVM3 yet)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
beacon-core = { path = "../beacon-core" }
beacon-events = { path = "../beacon-events" }
beacon-mock = { path = "../beacon-mock" }
bitcoin = { version = "0.32", default-features = false, features = ["std"] }
sha2 = "0.10"

[dev-dependencies]
beacon-groth16 = { path = "../beacon-groth16" }
ark-bn254 = "0.6"
