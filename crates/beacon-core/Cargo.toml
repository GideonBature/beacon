[package]
name = "beacon-core"
description = "Beacon assertion protocol — core types and traits (reference implementation)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
readme = "../../README.md"

[lints]
workspace = true

[dependencies]
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
